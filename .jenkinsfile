pipeline {
   agent any

   stages {
      stage('Building Hello World') {
         steps {

            //sh 'javac Hello.java' // not working
            //sh 'java Hello'   // not working
            bat 'java -version'

         }
      }

   stage('Deploy') {
         steps {
            echo 'Hello World deploy steps'
         }
      }
         stage('Test') {
         steps {
            echo 'Hello World Test steps'
         }
      }
       stage('Release') {
         steps {
            echo 'Hello World Release steps'
         }
      }
}
}
